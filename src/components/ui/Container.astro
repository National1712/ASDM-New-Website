---
interface Props {
  wide?: boolean;
  class?: string;
}

const { wide = false, class: className = '' } = Astro.props;
---

<div class={`container${wide ? '-wide' : ''} ${className}`}>
  <slot />
</div>
