<div
  class="hero-artwork-placeholder"
  role="img"
  aria-label="Final ASDM hero artwork pending"
>
  <div class="art-grid" aria-hidden="true"></div>
  <div class="orbit orbit-one" aria-hidden="true"></div>
  <div class="orbit orbit-two" aria-hidden="true"></div>

  <div class="studio-board">
    <div class="board-bar" aria-hidden="true">
      <span></span>
      <span></span>
      <span></span>
    </div>

    <div class="campaign-map" aria-hidden="true">
      <div class="map-node node-primary">SEO</div>
      <div class="map-node node-secondary">Ads</div>
      <div class="map-node node-tertiary">AI</div>
      <div class="map-path path-one"></div>
      <div class="map-path path-two"></div>
    </div>

    <div class="metric-card card-search" aria-hidden="true">
      <span>Search intent</span>
      <i></i>
      <i></i>
      <i></i>
    </div>

    <div class="metric-card card-content" aria-hidden="true">
      <span>Content plan</span>
      <div class="mini-calendar">
        <i></i>
        <i></i>
        <i></i>
        <i></i>
        <i></i>
        <i></i>
      </div>
    </div>

    <div class="chart-card" aria-hidden="true">
      <span>Analytics</span>
      <div class="chart-bars">
        <i></i>
        <i></i>
        <i></i>
        <i></i>
      </div>
    </div>

    <div class="safe-area">
      <span class="art-kicker">Artwork slot</span>
      <span class="art-label">Final ASDM Hero Artwork</span>
      <span class="art-note">Graphic designer asset pending</span>
    </div>
  </div>

  <div class="support-panel support-panel-one" aria-hidden="true"></div>
  <div class="support-panel support-panel-two" aria-hidden="true"></div>
  <div class="red-rule" aria-hidden="true"></div>
</div>

<style>
  .hero-artwork-placeholder {
    position: relative;
    aspect-ratio: 6 / 7;
    width: min(100%, 32rem);
    margin-left: auto;
    isolation: isolate;
    overflow: hidden;
    border-radius: var(--radius-lg);
    border: var(--border-width-hairline) solid rgba(199, 206, 216, 0.86);
    background:
      radial-gradient(
        circle at 86% 16%,
        rgba(101, 66, 155, 0.24),
        transparent 31%
      ),
      radial-gradient(
        circle at 5% 86%,
        rgba(225, 38, 47, 0.19),
        transparent 35%
      ),
      linear-gradient(145deg, #fff8f7 0%, #fbfbfd 48%, #f2eef8 100%);
    box-shadow:
      0 1.5rem 4rem rgba(17, 19, 26, 0.12),
      inset 0 0 0 1px rgba(255, 255, 255, 0.72);
  }

  .hero-artwork-placeholder::before {
    content: '';
    position: absolute;
    inset: 7% 8%;
    border-radius: var(--radius-lg);
    border: var(--border-width-hairline) solid rgba(101, 66, 155, 0.2);
    transform: rotate(-2deg);
    pointer-events: none;
  }

  .art-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(17, 19, 26, 0.055) 1px, transparent 1px),
      linear-gradient(90deg, rgba(17, 19, 26, 0.055) 1px, transparent 1px);
    background-size: 2rem 2rem;
    mask-image: linear-gradient(135deg, #000 0%, transparent 78%);
    opacity: 0.48;
  }

  .orbit {
    position: absolute;
    border-radius: 999px;
    border: var(--border-width-hairline) solid rgba(101, 66, 155, 0.18);
    pointer-events: none;
  }

  .orbit-one {
    width: 17rem;
    height: 17rem;
    top: -4rem;
    right: -3.5rem;
  }

  .orbit-two {
    width: 13rem;
    height: 13rem;
    left: -4rem;
    bottom: 4rem;
    border-color: rgba(225, 38, 47, 0.17);
  }

  .studio-board {
    position: absolute;
    inset: 10% 10% 14%;
    border-radius: var(--radius-lg);
    border: var(--border-width-hairline) solid rgba(17, 19, 26, 0.12);
    background:
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.96),
        rgba(255, 255, 255, 0.78)
      ),
      linear-gradient(135deg, rgba(225, 38, 47, 0.08), rgba(101, 66, 155, 0.11));
    box-shadow: 0 1.25rem 3.5rem rgba(17, 19, 26, 0.12);
  }

  .board-bar {
    position: absolute;
    inset: 0 0 auto;
    display: flex;
    gap: 0.35rem;
    height: 2.35rem;
    align-items: center;
    padding-inline: var(--sp-5);
    border-bottom: var(--border-width-hairline) solid rgba(17, 19, 26, 0.08);
  }

  .board-bar span {
    width: 0.45rem;
    height: 0.45rem;
    border-radius: 999px;
    background-color: rgba(17, 19, 26, 0.18);
  }

  .board-bar span:first-child {
    background-color: var(--brand-primary);
  }

  .campaign-map {
    position: absolute;
    top: 18%;
    left: 12%;
    width: 42%;
    height: 34%;
  }

  .map-node {
    position: absolute;
    z-index: 2;
    display: grid;
    place-items: center;
    width: 3.5rem;
    height: 3.5rem;
    border-radius: var(--radius-md);
    color: var(--text-primary);
    font-size: var(--fs-caption);
    font-weight: var(--fw-bold);
    background-color: rgba(255, 255, 255, 0.92);
    border: var(--border-width-hairline) solid rgba(17, 19, 26, 0.13);
    box-shadow: var(--shadow-soft);
  }

  .node-primary {
    left: 0;
    top: 0;
    color: var(--brand-primary);
  }

  .node-secondary {
    right: 4%;
    top: 28%;
    color: var(--brand-secondary);
  }

  .node-tertiary {
    left: 16%;
    bottom: -12%;
  }

  .map-path {
    position: absolute;
    height: 0.15rem;
    border-radius: var(--radius-pill);
    background: var(--gradient-brand);
    opacity: 0.78;
    transform-origin: left center;
  }

  .path-one {
    left: 22%;
    top: 31%;
    width: 54%;
    transform: rotate(17deg);
  }

  .path-two {
    left: 27%;
    bottom: 29%;
    width: 45%;
    transform: rotate(-23deg);
  }

  .metric-card,
  .chart-card {
    position: absolute;
    z-index: 3;
    border-radius: var(--radius-md);
    border: var(--border-width-hairline) solid rgba(17, 19, 26, 0.12);
    background-color: rgba(255, 255, 255, 0.86);
    box-shadow: 0 0.9rem 2rem rgba(17, 19, 26, 0.1);
    backdrop-filter: blur(12px);
  }

  .metric-card span,
  .chart-card span {
    display: block;
    color: var(--text-muted);
    font-size: var(--fs-caption);
    font-weight: var(--fw-semibold);
    line-height: var(--lh-meta);
  }

  .card-search {
    top: 12%;
    right: -4%;
    width: 38%;
    padding: var(--sp-4);
  }

  .card-search i {
    display: block;
    height: 0.38rem;
    margin-top: var(--sp-3);
    border-radius: var(--radius-pill);
    background-color: rgba(101, 66, 155, 0.17);
  }

  .card-search i:nth-child(3) {
    width: 72%;
    background-color: rgba(225, 38, 47, 0.17);
  }

  .card-search i:nth-child(4) {
    width: 48%;
  }

  .card-content {
    left: -6%;
    bottom: 18%;
    width: 34%;
    padding: var(--sp-4);
  }

  .mini-calendar {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.32rem;
    margin-top: var(--sp-3);
  }

  .mini-calendar i {
    aspect-ratio: 1;
    border-radius: 0.25rem;
    background-color: rgba(225, 38, 47, 0.12);
  }

  .mini-calendar i:nth-child(2),
  .mini-calendar i:nth-child(5) {
    background-color: rgba(101, 66, 155, 0.16);
  }

  .chart-card {
    right: 7%;
    bottom: 12%;
    width: 38%;
    padding: var(--sp-4);
  }

  .chart-bars {
    display: flex;
    align-items: end;
    gap: 0.38rem;
    height: 3.5rem;
    margin-top: var(--sp-3);
  }

  .chart-bars i {
    flex: 1;
    border-radius: var(--radius-pill) var(--radius-pill) 0 0;
    background: linear-gradient(
      180deg,
      var(--brand-primary),
      var(--brand-secondary)
    );
  }

  .chart-bars i:nth-child(1) {
    height: 48%;
  }

  .chart-bars i:nth-child(2) {
    height: 72%;
  }

  .chart-bars i:nth-child(3) {
    height: 56%;
  }

  .chart-bars i:nth-child(4) {
    height: 88%;
  }

  .safe-area {
    position: absolute;
    left: 34%;
    top: 41%;
    z-index: 4;
    width: 43%;
    min-height: 8.75rem;
    display: grid;
    align-content: center;
    justify-items: center;
    gap: var(--sp-2);
    padding: var(--sp-5);
    border: var(--border-width-hairline) dashed rgba(101, 66, 155, 0.42);
    border-radius: var(--radius-lg);
    text-align: center;
    background:
      linear-gradient(
        135deg,
        rgba(255, 255, 255, 0.88),
        rgba(255, 255, 255, 0.66)
      ),
      rgba(255, 255, 255, 0.36);
  }

  .art-kicker {
    color: var(--brand-primary);
    font-size: var(--fs-caption);
    font-weight: var(--fw-bold);
    line-height: var(--lh-meta);
    text-transform: uppercase;
  }

  .art-label {
    max-width: 12rem;
    color: var(--text-primary);
    font-size: var(--fs-label);
    font-weight: var(--fw-bold);
    letter-spacing: var(--ls-wide);
    line-height: var(--lh-meta);
    text-transform: uppercase;
  }

  .art-note {
    color: var(--text-muted);
    font-size: var(--fs-caption);
    line-height: var(--lh-meta);
  }

  .support-panel {
    position: absolute;
    border-radius: var(--radius-md);
    border: var(--border-width-hairline) solid rgba(17, 19, 26, 0.12);
    background-color: rgba(255, 255, 255, 0.42);
  }

  .support-panel-one {
    width: 32%;
    height: 14%;
    top: 19%;
    left: 5%;
  }

  .support-panel-two {
    width: 24%;
    height: 17%;
    right: 7%;
    bottom: 12%;
  }

  .red-rule {
    position: absolute;
    left: 16%;
    right: 16%;
    bottom: 10%;
    height: 0.2rem;
    border-radius: var(--radius-pill);
    background: var(--gradient-brand);
    opacity: 0.9;
  }

  @media (max-width: 1180px) {
    .hero-artwork-placeholder {
      width: min(100%, 29rem);
    }
  }

  @media (max-width: 768px) {
    .hero-artwork-placeholder {
      aspect-ratio: 4 / 5;
      width: min(100%, 23rem);
      margin-inline: auto;
      box-shadow: var(--shadow-soft);
    }

    .studio-board {
      inset: 9% 9% 13%;
    }

    .card-search {
      right: -7%;
      width: 42%;
    }

    .card-content {
      display: none;
    }

    .chart-card {
      width: 42%;
    }

    .safe-area {
      left: 24%;
      width: 56%;
    }

    .support-panel {
      display: none;
    }
  }
</style>
